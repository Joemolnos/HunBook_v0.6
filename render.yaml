services:
  # Backend API (FastAPI via Dockerfile)
  - type: web
    name: hunbook-api
    runtime: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /healthz
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: REQUIRE_BYOK
        value: "false"
      - key: QUOTA_PER_DAY
        value: "3"
      - key: FRONTEND_ORIGIN
        value: "https://hunbook-frontend.onrender.com, http://localhost:5173"

  # Frontend (Vite static build)
  - type: web
    name: hunbook-frontend
    runtime: static
    rootDir: web
    autoDeploy: true
    buildCommand: "npm ci && npm run build"
    staticPublishPath: "dist"
    envVars:
      - key: VITE_API_BASE
        fromService:
          name: hunbook-api
          type: web
          envVarKey: RENDER_EXTERNAL_URL
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
